# AutoMarkup.API
An application programming interface built specifically for application operations that provide Html &amp; CSS page building services using pages in addition to the code generation service and downloading the project as pre-built code by the application
